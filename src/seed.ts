/**
 * Seed module so the quality pipeline runs green from the first commit and
 * the initial baseline has something to measure. Delete it (and its test)
 * when your first real module lands.
 */
export function seed(a: number, b: number): number {
  return a + b;
}
